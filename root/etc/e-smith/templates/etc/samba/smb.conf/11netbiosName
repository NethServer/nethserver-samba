workgroup = { $smb{Workgroup} }
{
   my $ServerName = $smb{ServerName};

   if ( $ServerName ) {
      $OUT .= "netbios name = $ServerName";
   } else {
      $OUT .= "# netbios name = # default hostname";
   }
}
server string = { $smb{ServerString} || 'NethServer Samba %v' }
